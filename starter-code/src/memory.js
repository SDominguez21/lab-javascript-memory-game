class MemoryGame {
  constructor(card) {
    this.cards = card;
    this.pickedCards = []; //current card
    this.pairsClicked = 0;
    this.pairsGuessed = 0;
  }

  shuffleCards() {
    //     let random = 0;
    //     let temp = 0;
    // for(i = 1; i < card.length; i++){
  }

  checkIfPair() {
    alert("positive");
    //     add 1 to .pairsClicked
    //     and if cards are the same also add 1 to .pairsGuessed
    //   if (card1 == card2)
    //     {push.pairsClicked[] && return .pairsGuessed[+1]};
    //  };
  }

  isFinished() {}
} //end

console.log("TEST");
